<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="echipa_8.centenargo_app">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".activities.Login_Activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.Register_Activity" android:windowSoftInputMode="stateVisible|adjustResize" />
        <activity android:name=".activities.Routes_Activity" />
        <activity android:name=".activities.Route_Activity" />
        <activity android:name=".activities.Landmark_Activity" />
        <activity android:name=".activities.Questions_Activity" />
        <activity android:name=".activities.Upload_Activity" />
        <activity android:name=".activities.Gallery_Activity">

        </activity>
        <activity android:name=".activities.Image_Activity" />
        <activity android:name=".activities.Intro_Activity" />
    </application>

</manifest>